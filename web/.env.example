# CoughTB Environment Variables
# Copy this to .env and adjust values

# Port the server listens on (default: 8000)
PORT=3003

# CPU threads for PyTorch inference (default: 1)
# Set to 4 on VPS with 10GB+ RAM for faster inference
OMP_NUM_THREADS=4
